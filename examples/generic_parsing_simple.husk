// Simple test of generic type parameter parsing

// Generic function
fn identity<T>(value: T) -> T {
    value
}

// Generic struct
struct Box<T> {
    item: T,
}

// Generic enum
enum Result<T, E> {
    Ok(T),
    Err(E),
}

fn main() {
    let x = identity(42);
    println("Generic parsing test complete");
}