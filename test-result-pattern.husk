enum Test {
    Data { value: string }
}

fn main() {
    let r = Ok(Test::Data { value: "hello" });
    match r {
        Ok(Test::Data { value: v }) => println(v),
        Err(e) => println(e)
    }
}