---
const features = [
  {
    icon: 'syntax',
    title: 'Rust-like Syntax',
    description: 'Familiar syntax with pattern matching, algebraic data types, and powerful type inference. If you know Rust, you\'ll feel right at home.',
    color: 'husk',
  },
  {
    icon: 'shield',
    title: 'Type Safety',
    description: 'Catch errors at compile time with a robust type system. Strong static typing with inference means safety without verbosity.',
    color: 'terra',
  },
  {
    icon: 'bolt',
    title: 'Developer Experience',
    description: 'Fast compile times, clear error messages, and excellent tooling. LSP support, code formatting, and editor integrations included.',
    color: 'husk',
  },
  {
    icon: 'link',
    title: 'JavaScript Interop',
    description: 'Seamlessly call npm packages and JavaScript libraries. Compiles to clean ES modules that integrate with any JS project.',
    color: 'terra',
  },
];
---

<section
  id="features"
  class="relative py-24 sm:py-32 bg-husk-100/30 dark:bg-surface-800/50"
>
  <!-- Section header -->
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mb-16">
    <div class="text-center max-w-3xl mx-auto">
      <h2 class="font-display text-4xl sm:text-5xl md:text-6xl text-surface-900 dark:text-cream-50 mb-6">
        Why <span class="text-husk-600 dark:text-husk-400 italic">Husk</span>?
      </h2>
      <p class="text-lg sm:text-xl text-surface-600 dark:text-cream-100/80 leading-relaxed">
        The power of Rust's type system, the reach of JavaScript's ecosystem.
        Write once, run anywhere — with confidence.
      </p>
    </div>
  </div>

  <!-- Feature cards -->
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 lg:gap-8">
      {features.map((feature, index) => (
        <article
          class="feature-card group relative bg-cream-50 dark:bg-surface-800 rounded-2xl p-8 lg:p-10 border border-cream-200 dark:border-surface-700 card-hover overflow-hidden"
          style={`--delay: ${index * 100}ms`}
        >
          <!-- Background gradient on hover -->
          <div class={`absolute inset-0 opacity-0 group-hover:opacity-100 transition-opacity duration-500 bg-gradient-to-br ${feature.color === 'husk' ? 'from-husk-100/50 to-transparent dark:from-husk-900/20' : 'from-terra-400/10 to-transparent dark:from-terra-600/10'}`}></div>

          <!-- Icon -->
          <div class={`relative z-10 w-14 h-14 rounded-xl flex items-center justify-center mb-6 ${feature.color === 'husk' ? 'bg-husk-100 dark:bg-husk-900/50 text-husk-600 dark:text-husk-400' : 'bg-terra-400/20 dark:bg-terra-600/20 text-terra-500 dark:text-terra-400'}`}>
            {feature.icon === 'syntax' && (
              <svg class="w-7 h-7" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                <path d="M17.25 6.75L22.5 12l-5.25 5.25m-10.5 0L1.5 12l5.25-5.25m7.5-3l-4.5 16.5" />
              </svg>
            )}
            {feature.icon === 'shield' && (
              <svg class="w-7 h-7" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                <path d="M9 12.75L11.25 15 15 9.75m-3-7.036A11.959 11.959 0 0 1 3.598 6 11.99 11.99 0 0 0 3 9.749c0 5.592 3.824 10.29 9 11.623 5.176-1.332 9-6.03 9-11.622 0-1.31-.21-2.571-.598-3.751h-.152c-3.196 0-6.1-1.248-8.25-3.285Z" />
              </svg>
            )}
            {feature.icon === 'bolt' && (
              <svg class="w-7 h-7" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                <path d="m3.75 13.5 10.5-11.25L12 10.5h8.25L9.75 21.75 12 13.5H3.75Z" />
              </svg>
            )}
            {feature.icon === 'link' && (
              <svg class="w-7 h-7" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                <path d="M13.19 8.688a4.5 4.5 0 0 1 1.242 7.244l-4.5 4.5a4.5 4.5 0 0 1-6.364-6.364l1.757-1.757m13.35-.622 1.757-1.757a4.5 4.5 0 0 0-6.364-6.364l-4.5 4.5a4.5 4.5 0 0 0 1.242 7.244" />
              </svg>
            )}
          </div>

          <!-- Content -->
          <div class="relative z-10">
            <h3 class="font-display text-2xl lg:text-3xl text-surface-900 dark:text-cream-50 mb-3">
              {feature.title}
            </h3>
            <p class="text-surface-600 dark:text-cream-100/80 leading-relaxed text-lg">
              {feature.description}
            </p>
          </div>

          <!-- Decorative corner -->
          <div class={`absolute -bottom-4 -right-4 w-24 h-24 rounded-full opacity-10 group-hover:opacity-20 transition-opacity ${feature.color === 'husk' ? 'bg-husk-500' : 'bg-terra-500'}`}></div>
        </article>
      ))}
    </div>
  </div>

  <!-- Bottom highlight bar -->
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-16">
    <div class="relative flex flex-col md:flex-row items-center justify-center gap-8 md:gap-16 py-8 border-t border-cream-200 dark:border-surface-700">
      <div class="flex items-center gap-3">
        <svg class="w-6 h-6 text-husk-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        <span class="text-surface-600 dark:text-cream-100/80">Full LSP Support</span>
      </div>
      <div class="flex items-center gap-3">
        <svg class="w-6 h-6 text-husk-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        <span class="text-surface-600 dark:text-cream-100/80">VS Code & Neovim</span>
      </div>
      <div class="flex items-center gap-3">
        <svg class="w-6 h-6 text-husk-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        <span class="text-surface-600 dark:text-cream-100/80">Source Maps</span>
      </div>
      <div class="flex items-center gap-3">
        <svg class="w-6 h-6 text-husk-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        <span class="text-surface-600 dark:text-cream-100/80">Watch Mode</span>
      </div>
    </div>
  </div>
</section>

<style>
  .feature-card {
    opacity: 0;
    animation: fadeInUp 0.6s cubic-bezier(0.16, 1, 0.3, 1) forwards;
    animation-delay: var(--delay);
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
