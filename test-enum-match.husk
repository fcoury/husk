enum Command {
    Process {
        input: string,
        output: string,
    },
    Help,
}

fn test(cmd: Command) {
    match cmd {
        Command::Process { input, output } => println("Processing"),
        Command::Help => println("Help"),
    }
}