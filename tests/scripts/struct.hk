struct Person {
    name: string,
    age: int,
}

let name = "Felipe";
let age = 44;
let person = Person { name, age };
println(person.name);
println(person.age);
println(person);

person.age = 22;
println(person.age);
