enum Command {
    Process { input: string },
}

fn main() {
    let cmd = 42;
    match cmd {
        Command::Process { input } => {
            println("Got: {}", input);
        },
        _ => {
            println("Other");
        }
    }
}